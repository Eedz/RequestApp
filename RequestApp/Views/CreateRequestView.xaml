<UserControl x:Class="RequestApp.CreateRequestView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:RequestApp"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="700" Background="{StaticResource AppBackgroundBrush}">
    <Grid Margin="3">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="110"/>
            <ColumnDefinition Width="300"/>
            <ColumnDefinition Width="100"/>
            <ColumnDefinition Width="50"/>
            <ColumnDefinition Width="100"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="30"/>
            <RowDefinition Height="25"/>
            <RowDefinition Height="25"/>
            <RowDefinition Height="25"/>
            <RowDefinition Height="25"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel></StackPanel>

        <TextBlock Text="Requester" Grid.Row="0" Grid.Column="0" FontWeight="Bold" Margin="3"/>

        <DockPanel Grid.Column="1" >
            <TextBox x:Name="RequesterTextBox"  Text="{Binding Requester.FullName, Mode=OneWay}" d:Text="FirstName LastName" Width="250" FontSize="18" DockPanel.Dock="Left" Margin="3"/>
            <Button Content="V" Command="{Binding ShowNamesCommand}" Margin="3"/>

        </DockPanel>
        <Popup IsOpen="{Binding DropDownNames}" PlacementTarget="{Binding ElementName=RequesterTextBox}">
            <ListBox Height="100" Width="300" ItemsSource="{Binding Requesters}" SelectedItem="{Binding Requester, Mode=TwoWay}">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <TextBlock Text="{Binding FirstName}"/>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
        </Popup>


        <TextBlock Text="Authorized By:" Grid.Row="1" Grid.Column="0" FontWeight="Bold" Margin="3"/>
        <TextBox Text="{Binding AuthorizedBy}" Grid.Column="1" Grid.Row="1"/>
        <TextBlock Text="Expiry Date:" Grid.Row="2" Grid.Column="0" FontWeight="Bold" Margin="3"/>
        <DatePicker SelectedDate="{Binding ExpiryDate}" Grid.Column="1" Grid.Row="2" />
        <TextBlock Text="Last Signed Date:" Grid.Row="3" Grid.Column="0" FontWeight="Bold" Margin="3"/>
        <DatePicker SelectedDate="{Binding LatestSigning}" Grid.Column="1" Grid.Row="3"/>
        <TextBlock Text="Internal/External:" Grid.Row="4" Grid.Column="0" FontWeight="Bold" Margin="3"/>
        <ComboBox SelectedItem="{Binding InternalExternal}" ItemsSource="{Binding InternalExternalItems}" Grid.Column="1" Grid.Row="4"/>
        <TextBlock Text="Notes:" Grid.Row="5" Grid.Column="0" FontWeight="Bold" Margin="3"/>
        <TextBox Text="{Binding Notes}" Grid.Column="1" Grid.Row="5" Height="100" AcceptsReturn="True" TextWrapping="Wrap"/>

        <StackPanel Grid.Row="8" Grid.Column="1">
            <Button Content="Save" Command="{Binding SaveCommand}" Margin="5"/>
            <Button Content="Cancel" Command="{Binding CancelCommand}" Margin="5"/>
        </StackPanel>
        
        <StackPanel Grid.Column="2" >
            <TextBlock Text="Data Sets" FontWeight="Bold" HorizontalAlignment="Center"/>
            
        </StackPanel>
        <ListBox  Grid.Column="2" Grid.Row="1" Grid.RowSpan="8" ItemsSource="{Binding DataSets}" Margin="3" Height="200" VerticalAlignment="Top">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="{Binding Name}" Margin="5"/>
                        <Button Content="Remove" Command="{Binding DataContext.RemoveDataSetCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" CommandParameter="{Binding}"/>
                    </StackPanel>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>


        <Button Grid.Column="3" Grid.Row="1" Content="&lt;&lt;" Margin="0" VerticalContentAlignment="Center" Command="{Binding AddDataSetCommand}" CommandParameter="{Binding SelectedItem, ElementName=AvailableDataSetsListBox}"/>
        <Button Grid.Column="3" Grid.Row="2" Content=">>" Margin="0" Command="{Binding RemoveDataSetCommand}" CommandParameter="{Binding SelectedItem, ElementName=DataSetsListBox}"/>

        <ListBox x:Name="AvailableDataSetsListBox" Grid.Column="4" Grid.Row="1" Grid.RowSpan="8" ItemsSource="{Binding AvailableDataSets}" Margin="3" Height="200" VerticalAlignment="Top">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <TextBlock Text="{Binding Name}" Margin="5"/>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
    </Grid>
</UserControl>
